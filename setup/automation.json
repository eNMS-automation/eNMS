{
  "workflow": {
    "allowed_models" : {
      "delete": ["device", "link", "pool"],
      "fetch": ["device", "link", "pool", "service"],
      "fetch_all": ["device", "link", "pool", "service"],
      "factory": ["device", "link", "pool"]
    }
  }
}
