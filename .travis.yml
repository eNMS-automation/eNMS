language: python

python:
  - 2.7
  - 3.4
  - 3.5
  - 3.6

install:
  - pip install coveralls
  - pip install -r requirements.txt

before_script:
  cd tests/

script:
  - coverage run --source=../source -m pytest

after_success:
  - coveralls
